# Быстрый старт бота за 5 минут

## Шаг 1: Получить токен бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду: `/newbot`
4. Введите имя: `Assist Support Bot`
5. Введите username: `Assist_SupportBot` (или придумайте свой)
6. **Скопируйте токен** (выглядит как `123456:ABCdef...`)

## Шаг 2: Настроить проект (1 минута)

1. Скопируйте `.env.example` в `.env`:

**Windows:**
```bash
copy .env.example .env
```

**Mac/Linux:**
```bash
cp .env.example .env
```

2. Откройте файл `.env` и вставьте токен:

```
BOT_TOKEN=ваш_токен_сюда
```

## Шаг 3: Установить зависимости (1 минута)

Откройте терминал в папке проекта:

```bash
cd "d:\Danila AI\projects\active\tg_bot_Assist"
```

Установите библиотеки:

```bash
pip install -r requirements.txt
```

## Шаг 4: Запустить бота (30 секунд)

```bash
python bot.py
```

Вы должны увидеть:

```
✅ База данных инициализирована
✅ Бот успешно запущен и готов к работе!
```

## Шаг 5: Протестировать (1 минута)

1. Откройте Telegram
2. Найдите вашего бота по username
3. Нажмите `/start`
4. Проверьте все кнопки:
   - ✅ Условия доступа
   - ✅ Об авторах канала
   - ✅ Задать вопрос
   - ✅ Выбор тарифов
   - ✅ Кнопка "Написать менеджеру"

---

## Возможные проблемы

### Ошибка: "No module named 'aiogram'"

**Решение:**
```bash
pip install aiogram
```

### Ошибка: "Токен бота не указан!"

**Решение:**
1. Проверьте что создали файл `.env` (не `.env.example`!)
2. Проверьте что токен вставлен без пробелов
3. Проверьте что строка начинается с `BOT_TOKEN=`

### Бот не отвечает

**Решение:**
1. Проверьте что скрипт `bot.py` запущен (должны видеть "Бот запущен")
2. Проверьте токен - возможно скопировали неправильно
3. Отправьте `/start` в чат с ботом

---

## Что дальше?

- 📖 Прочитайте [README.md](README.md) для подробной информации
- 🚀 Посмотрите [ДОПОЛНИТЕЛЬНЫЕ_ФУНКЦИИ.md](ДОПОЛНИТЕЛЬНЫЕ_ФУНКЦИИ.md) для расширения функционала
- 🌐 Подготовьте бота к публикации (деплой на сервер)

---

## Остановка бота

Нажмите `Ctrl+C` в терминале где запущен бот.

---

Всё готово! Бот работает локально на вашем компьютере. Для работы 24/7 нужно будет разместить на сервере (инструкции позже).
