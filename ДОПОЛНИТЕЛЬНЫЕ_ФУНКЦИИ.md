# Дополнительные функции (опционально)

Функции из ТЗ, которые можно добавить при необходимости.

## 1. Исчезающие сообщения

**Что это:** Сообщения бота автоматически удаляются после прочтения или через определенное время.

**Как реализовать:**

### Вариант А: Удаление предыдущего сообщения при нажатии кнопки

В файле `handlers/callbacks.py` добавьте в каждый обработчик:

```python
async def callback_tariffs(callback_query: types.CallbackQuery):
    user_id = callback_query.from_user.id
    log_action(user_id, 'view_tariffs')

    # Удаляем предыдущее сообщение
    try:
        await callback_query.message.delete()
    except Exception:
        pass  # Игнорируем ошибки если сообщение уже удалено

    # Отправляем новое сообщение
    await callback_query.message.answer(
        text=TARIFFS_MESSAGE,
        reply_markup=get_tariffs_keyboard(),
        parse_mode='HTML'
    )
    await callback_query.answer()
```

### Вариант Б: Удаление через время

```python
import asyncio

async def callback_tariffs(callback_query: types.CallbackQuery):
    # Отправляем сообщение
    msg = await callback_query.message.edit_text(...)

    # Удаляем через 60 секунд
    await asyncio.sleep(60)
    try:
        await msg.delete()
    except Exception:
        pass
```

**Внимание:** Telegram не поддерживает встроенные "исчезающие сообщения" для ботов, только ручное удаление.

---

## 2. Сбор базы контактов (запрос номера телефона)

**Что это:** Пользователь может поделиться своим номером телефона через кнопку "Поделиться контактом".

**Как реализовать:**

### Шаг 1: Создать клавиатуру с кнопкой "Поделиться контактом"

В файле `keyboards/inline.py` добавьте новую клавиатуру:

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton

def get_contact_request_keyboard():
    """
    Клавиатура для запроса контакта
    """
    keyboard = ReplyKeyboardMarkup(resize_keyboard=True, one_time_keyboard=True)
    contact_button = KeyboardButton(
        text="📱 Поделиться контактом",
        request_contact=True
    )
    skip_button = KeyboardButton(text="⏭ Пропустить")
    keyboard.add(contact_button)
    keyboard.add(skip_button)
    return keyboard
```

### Шаг 2: Обработчик запроса контакта

В файле `handlers/start.py` добавьте после приветствия:

```python
async def cmd_start(message: types.Message):
    # ... существующий код ...

    # Отправляем сообщение с главным меню
    await message.answer(
        text=welcome_text,
        reply_markup=get_main_menu_keyboard(),
        parse_mode='HTML'
    )

    # Просим поделиться контактом (опционально)
    await message.answer(
        text="Чтобы мы могли связаться с вами, поделитесь своим контактом:",
        reply_markup=get_contact_request_keyboard()
    )
```

### Шаг 3: Обработчик получения контакта

Создайте новый файл `handlers/contact.py`:

```python
# -*- coding: utf-8 -*-
from aiogram import types
from aiogram.dispatcher import Dispatcher
import sqlite3
from config import DATABASE_PATH

async def contact_handler(message: types.Message):
    """
    Обработка полученного контакта пользователя
    """
    if message.contact:
        user_id = message.from_user.id
        phone_number = message.contact.phone_number

        # Сохраняем номер телефона в базу данных
        conn = sqlite3.connect(DATABASE_PATH)
        cursor = conn.cursor()

        cursor.execute('''
            UPDATE users
            SET phone_number = ?
            WHERE user_id = ?
        ''', (phone_number, user_id))

        conn.commit()
        conn.close()

        await message.answer(
            "Спасибо! Ваш контакт сохранен 📱",
            reply_markup=types.ReplyKeyboardRemove()  # Убираем кнопки
        )


async def skip_contact_handler(message: types.Message):
    """
    Пользователь пропустил предоставление контакта
    """
    await message.answer(
        "Хорошо, продолжаем без контакта 👌",
        reply_markup=types.ReplyKeyboardRemove()
    )


def register_contact_handlers(dp: Dispatcher):
    dp.register_message_handler(contact_handler, content_types=['contact'])
    dp.register_message_handler(skip_contact_handler, lambda msg: msg.text == "⏭ Пропустить")
```

### Шаг 4: Обновить базу данных

В файле `database/db.py` добавьте поле `phone_number` в таблицу users:

```python
cursor.execute('''
    CREATE TABLE IF NOT EXISTS users (
        user_id INTEGER PRIMARY KEY,
        username TEXT,
        first_name TEXT,
        last_name TEXT,
        phone_number TEXT,  -- ДОБАВИТЬ ЭТУ СТРОКУ
        first_interaction TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        last_interaction TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
''')
```

### Шаг 5: Зарегистрировать обработчики

В файле `bot.py` добавьте:

```python
from handlers.contact import register_contact_handlers

# ...

def main():
    # ...
    register_start_handlers(dp)
    register_callback_handlers(dp)
    register_contact_handlers(dp)  # ДОБАВИТЬ ЭТУ СТРОКУ
    # ...
```

---

## Когда добавлять эти функции?

1. **Исчезающие сообщения** - можно добавить сразу, если хотите чтобы чат был чище
2. **Сбор контактов** - добавьте когда:
   - Клиенту понадобится база номеров телефонов
   - Захотите делать персональные рассылки
   - Менеджеру нужно будет связываться по телефону

**Важно:** Спросите у клиента нужны ли эти функции сейчас или можно добавить потом!

---

## Как тестировать

После добавления функций:

1. Перезапустите бота: `python bot.py`
2. Отправьте `/start` в чат с ботом
3. Проверьте работу новых функций

---

## Нужна помощь?

Если возникнут вопросы при добавлении функций - напишите, помогу разобраться!
