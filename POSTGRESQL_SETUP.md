# Настройка PostgreSQL для постоянного хранения данных

## Проблема

На Bothost при использовании SQLite база данных хранится внутри контейнера и **удаляется при каждом Rebuild/Redeploy**. Контакты пользователей теряются.

## Решение: PostgreSQL

PostgreSQL хранится отдельно от контейнера и **НИКОГДА не удаляется** при перезапусках бота.

---

## Шаг 1: Узнайте переменные PostgreSQL от Bothost

Напишите в поддержку Bothost:

> "Какие переменные окружения для подключения к PostgreSQL доступны в контейнере? Нужны: хост, порт, имя БД, пользователь, пароль. Или есть готовая DATABASE_URL?"

Обычно они предоставляют один из вариантов:

### Вариант А: DATABASE_URL
```
DATABASE_URL=postgresql://user:password@host:5432/dbname
```

### Вариант Б: Отдельные переменные
```
POSTGRES_HOST=pg-xxx.bothost.ru
POSTGRES_PORT=5432
POSTGRES_DB=bot_database
POSTGRES_USER=bot_user
POSTGRES_PASSWORD=secure_password
```

---

## Шаг 2: Добавьте переменные в Bothost

1. Откройте настройки бота на Bothost
2. Перейдите в **"Переменные окружения"**
3. Добавьте переменные которые дала поддержка

**Важно:** Добавляйте **ТОЧНО ТАК** как их назвала поддержка!

---

## Шаг 3: Пересоберите бота

1. В панели Bothost нажмите **"Redeploy"** или **"Rebuild"**
2. Дождитесь окончания сборки (2-3 минуты)

---

## Шаг 4: Проверьте логи

После запуска в логах должно быть:

```
✅ База данных инициализирована (PostgreSQL)
✅ Бот успешно запущен и готов к работе!
```

Если видите `(PostgreSQL)` - всё работает! Контакты теперь сохраняются навсегда.

Если видите `(SQLite)` - PostgreSQL переменные не подключились, проверьте их названия.

---

## Как работает

Код бота **автоматически определяет** какую БД использовать:

1. ✅ Если есть `DATABASE_URL` или `POSTGRES_*` переменные → PostgreSQL
2. ⬇️ Если нет → SQLite (локально для разработки)

**PostgreSQL переменные:**
- `DATABASE_URL` (приоритет)
- или `POSTGRES_HOST` + `POSTGRES_PORT` + `POSTGRES_DB` + `POSTGRES_USER` + `POSTGRES_PASSWORD`

---

## Частые вопросы

### Q: Потеряются ли текущие контакты?

A: Если в SQLite уже есть контакты - экспортируйте их через `/admin` → "Экспорт" **ДО** миграции на PostgreSQL. После миграции контакты из SQLite не перенесутся автоматически.

### Q: Можно ли использовать свой PostgreSQL сервер?

A: Да! Укажите свои credentials в переменных окружения. Бот работает с любым PostgreSQL 12+.

### Q: Как проверить что используется PostgreSQL?

A: Посмотрите логи бота при запуске. Должно быть:
```
✅ База данных инициализирована (PostgreSQL)
```

### Q: SQLite всё ещё работает?

A: Да! Локально (на вашем компьютере) бот использует SQLite для удобства разработки. На Bothost с PostgreSQL переменными - PostgreSQL.

---

## Примечания

- PostgreSQL автоматически создаёт таблицы при первом запуске
- Все данные хранятся **навсегда** - не удаляются при Rebuild
- Можно в любой момент переключаться между SQLite и PostgreSQL
- PostgreSQL поддерживает большие объёмы данных (миллионы записей)

---

**Готово!** После настройки контакты будут сохраняться навсегда 🎉
